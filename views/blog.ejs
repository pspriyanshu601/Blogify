<!DOCTYPE html>
<html lang="en">
  <head>
   <%- include("./partials/head.ejs") %>
    <title>Document</title>
  </head>
  <body>
   <%-include("./partials/script.ejs")%>
   <%- include("./partials/nav.ejs") %>
   <div class="conatiner mt-4">
        <h1><%= blog.title %></h1>
        <img src="<%= blog.coverImageURL %>" width="900px">
        <pre><%= blog.body %></pre>
   </div>

   <div class="conatiner mt-4">
        <img src="<%= blog.createdBy.profileImageURL %>" alt="lol" width="50px"><%= blog.createdBy.fullName %>
   </div>

   </div>
  </body>
</html>
